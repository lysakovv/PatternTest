Документация к генератору паттернов:

Возможности:
- Выбор количества вопросов в опросе

Поля ввода пользователя:
- Количество вопросов(int)
- Вариант ответа на открывшуюся картинку с описанием паттерна

Классы:
- Main \\Класс, отвечающий за взаимодействие с  пользователем, запуск опроса с указанным количеством вопросов
- DataLoader \\Класс, отвечающий за действия с данными проекта(картинки паттернов и правильные к ним ответы)
- Pattern \\Класс для хранения данных о текущем паттерне
- DataLoaderTest, SurveyCreationTest, AnswerCorrectnessTest - классы с тестами
